export const col = (data) => `<div class="col-md">${data}</div>`
export const row = (data) => `<div class="row">${data}</div>`

export const btn = (data) => `<button class="btn btn-primary col">${data}</button>`